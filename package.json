{
  "name": "tempo-rhythm",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "node scripts/ensure-biome-windows-cli.mjs",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=tempo-rhythm-web",
    "dev:mobile": "turbo run dev --filter=tempo-rhythm-mobile",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "test": "turbo run test",
    "clean": "turbo run clean && bun x rimraf node_modules .turbo apps/*/node_modules apps/*/.next apps/*/.turbo apps/*/.expo packages/*/node_modules packages/*/.turbo",
    "convex:dev": "bun x convex dev",
    "convex:deploy": "bun x convex deploy",
    "convex:codegen": "bun x convex codegen"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.91",
    "convex": "^1.32.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/node": "^20.19.0",
    "turbo": "^2.3.3"
  }
}

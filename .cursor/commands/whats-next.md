# What's next?

Follow the **session-start** workflow from `.cursor/rules/session-start.mdc`:

1. Read `docs/brain/TASKS.md`.
2. Pick **three** `todo` tasks with satisfied dependencies (and quick sanity-check against the repo).
3. Output them as a numbered list: **T-xxxx — title** plus one short line each.
4. Do **not** write code until I say which task to implement.

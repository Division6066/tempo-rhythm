# Tick off tasks

Follow **task-complete** from `.cursor/rules/task-complete.mdc`:

1. I confirm which task ID(s) (e.g. T-0012) are done after this session.
2. Update `docs/brain/TASKS.md` status for those rows.
3. Summarize what changed in the repo (short bullets).
4. Propose the **next three** tasks from `docs/brain/TASKS.md` the same way as `/whats-next`.

# tempo-rhythm open-PR triage — 2026-08-13

Agent: copilot triage run. **No merges. No closes. No auto-merge. No workflow edits. No `convex/schema.ts` edits.**

**Working trunk:** `integration` @ `491f2a6` (includes merged #321).  
**`master`:** human-only @ `66a3848`. Do not target.

**Scope note:** agent-writie Task B ignored per run instruction.

**Queue handling note:** Only the ranked top-10 candidates were examined for un-stale merges. **The other ~90 open PRs were inventoried and classified but NOT refreshed against `integration`.** Do not treat the queue as fully un-staled.

---

## 0. Snapshot

| Metric | Value |
|---|---|
| Open PRs | **100** |
| Base = `integration` | **6** (#324, #323, #322, #318, #317, #304) |
| Base = `master` (wrong trunk) | **94** |
| Draft | 92 |
| Ready (non-draft) | 8 |
| Age > 14 days | 94 (all of them are `master`-based) |
| Stale update > 14 days | 93 |
| #321 status | **MERGED** into `integration` 2026-08-13T07:30:05Z (was listed as queued; already landed) |

Authors: Division6066 70 · cursor[bot] 22 · nebula-ai-agent[bot] 4 · dependabot[bot] 3 · Copilot 1

---

## 1. Ranked merge order (top 10)

Only **five** PRs are realistically mergeable in one human sitting against `integration` with green checks and no mutual file overlap. Positions 6–10 are **empty / not merge-ready** — filled with explicit “do not merge yet” slots rather than inventing false readiness.

| Rank | PR | Base | Draft | Checks (required names) | Diff | Why this position |
|---|---|---|---|---|---|---|
| **1** | **#324** fix(analytics): missing `returns` on `analytics.overview` | `integration` | yes | Lint/Test/Typecheck/**all green** | +185/−32, 2 files | Smallest green validator fix; zero overlap with other integration PRs; closes the last bare public query; pure scalars so no schema-field omission risk. **Do not change this PR (queued).** |
| **2** | **#318** fix(tasks): normalize `projectSlug` | `integration` | yes | Lint/Test/Typecheck/**all green** | +45/−2, 2 files | Tiny bugfix; merges clean onto current `integration` locally; no overlap with #322–324. (Overlaps abandoned #228 only.) |
| **3** | **#323** feat(today): energy-aware recommendations (T-007c) | `integration` | yes | Lint/Test/Typecheck/**all green** | +481/0, 4 files | Green feature; **no new Convex functions**; disjoint files from #324/#322/#318. Supersedes intent of stale #171/#227 for the *task* side. **Do not change this PR (queued).** |
| **4** | **#322** feat(insights): `insights.summary` + live screen | `integration` | yes | Lint/Test/Typecheck/**all green** | +653/−13, 5 files | Green; new Convex query with full `returns:`; disjoint from #323/#324/#318. Larger than #323 so later. **Do not change this PR (queued).** |
| **5** | **#317** fix: Bugbot secret-scan fixture exit codes | `integration` | **no** | Lint/Test/Typecheck/**all green** | +27/−5, 2 files | Real CI-teeth fix in `scripts/secret-scan-test-fixture.ts`, **but also edits `.github/workflows/security.yml`** (gitleaks checksum install). Workflow freeze means a **human** must accept the workflow hunk or strip it. Ranked last among greens for that reason. |
| 6 | — | — | — | — | — | **No further integration-based green PRs.** |
| 7 | #304 chore(devops): Vercel preview pipe | `integration` | yes | Only `Vercel Preview Comments` success; **no CI Lint/Test/Typecheck runs on head** | **0 files** | Empty / plumbing-only; share URL expired 2026-08-03. **Recommend close**, not merge. |
| 8 | #230 dependabot setup-node 4→7 | `master` | no | Green on old master CI | workflow-only | Wrong base; **touches `.github/workflows/**` — do not merge as-is.** |
| 9 | #120 dependabot checkout 4→7 | `master` | no | Green on old master CI | workflow-only | Wrong base; **touches workflows — do not merge as-is.** |
| 10 | #133 `@tempo/backend` package | `master` | no | Green on old master CI | +3598, 39 files | Wrong base; integrator cutover PR; conflicts with root `convex/` reality on `integration`. **Do not merge without explicit cutover plan.** |

### Suggested human merge sequence (one sitting)

```
integration
  └─ squash/merge #324
  └─ squash/merge #318
  └─ squash/merge #323
  └─ squash/merge #322
  └─ human-decide #317 (workflow hunk)
```

All four of #324/#318/#323/#322 are **pairwise file-disjoint** on meaningful paths. Order among 324/318/323/322 can be permuted; smallest-first is above.

### Green ≠ fully tested

`bun test` / CI Test job scope is `convex`, `apps/web/lib`, `tests/unit` only. **Playwright specs under `tests/e2e` are not run by bun and are not in CI.** A green PR is not an e2e-tested PR. #322/#323/#318 ship unit coverage; e2e remains human/manual.

---

## 2. Close recommendations (recommend only — nothing closed)

### A. Already landed / empty

| PR | Reason |
|---|---|
| **#304** | Empty diff; Vercel pipe proof already done; share link expired. Superseded by living `integration` previews. |
| **#321** | **Already merged** into `integration` (not open). |

### B. Disposable / explicit throwaway

| PR | Reason |
|---|---|
| **#132** | Title says disposable orchestrator shakeout — safe to delete. File: `docs/qwen-trial-1.md` only. |

### C. Duplicate security / auth fix family (keep at most one if any; prefer re-implement on `integration`)

Near-identical soft-delete / payment-bypass / account-deletion patches. All base=`master`, age 49–68d, superseded in spirit by #7/#19/#315 work already on `integration`. **Recommend close all; open a fresh audit PR on `integration` if gaps remain.**

| PRs | Superseding reality |
|---|---|
| #131, #128, #127, #124, #123, #119, #115, #103, #76, #55, #47 | Landed soft-delete + auth on `integration` (`convex/schema.ts` deletedAt, `requireUser`, etc.). Latest duplicate cluster tip historically #131. |
| #118 | Soft-delete + auth (nebula); same theme; red Lint+Typecheck on old master. Close; re-audit on `integration`. |
| #117, #116 | Duplicate “T-002 Auth And Approved User Gate” (same title). #116 green, #117 green, both base=`master`, 52d stale. Auth gate already on `integration` via #19. **Close both.** |

### D. Duplicate test-coverage family

| PRs | Reason |
|---|---|
| #130, #126, #122, #114, #110, #53, #48 | Repeated “missing test coverage” / regression suites on `master`. Close; write tests against current `integration` APIs. |

### E. Duplicate docs family

| PRs | Reason |
|---|---|
| #129, #125, #113, #109 | Repeated “align developer docs with Bun/Mistral/CI/EAS”. Close; single docs PR on `integration` if still needed. #134 (readme looptest header) also stale docs-only on `master`. |

### F. Feature work already partially landed by #315 / trunk (base=`master`, abandoned >14d)

| PRs | Reason / superseding |
|---|---|
| #228, #206, #170 | Core task views / variants / kanban — **#315** landed T-005a task views + schema on `integration`. #318 is the live follow-up bugfix. Close #228/#206/#170. |
| #226, #225, #203, #173 | Calendar variants / shared source / reschedule / auto-schedule — **#315** landed T-006a calendar source. Close or rewrite on `integration`. |
| #227, #171 | Habits energy suggestions — superseded for *task* energy UI by **#323**; habit-side still open product question but these branches are 29d+ on `master`. Close and reopen on `integration` if still wanted. |
| #223 | Brain-dump day-one — brain dump already on trunk (`TodayBrainDumpPanel`, `convex/brain_dump.ts`). Close unless diff still unique (verify before close). |

### G. Dependabot / workflow PRs (wrong base + freeze)

| PR | Reason |
|---|---|
| #302 | Workspace dep bump (35 packages); base=`master`; **Lint/Typecheck/Test/Scans failed**. Do not merge; recreate against `integration` after green. |
| #230, #120 | Action major bumps; touch `.github/workflows/**`. Wrong base. Human-only if ever; not agent work. |

### H. Bulk abandoned master drafts (>14d, no path to `integration`)

All remaining open PRs with base=`master` and `updated_at` ≥14d ago (mobile session/breathwork/vault/tickets/RTL/theme/etc., roughly **#47–#229 except items already listed**) are **abandoned relative to the current trunk**. Recommend mass-close with a single message: *“Trunk is `integration`; please reopen against `integration` if still needed.”*  
Specific high-noise mobile clusters: #222 #221 #218 #215 #214 #212 #211 #208 #207 #205 #204 #202 #201 #200 #199 #198 #197 #196 #195 #194 #193 #192 #191 #190 #189 #188 #187 #186 #185 #184 #183 #182 #181 #180 #179 #178 #177 #176 #175 #174 #172 #137 #136 #79.

**Count recommend-close:** ~94 master-based + #304 ≈ **95** close candidates if applying the abandoned-trunk rule strictly. Human should batch-close, not agents.

---

## 3. File-overlap map

Noise paths excluded from “meaningful” overlap: `bun.lock`, root/`apps/*/package.json`, `playwright.config.*`, `tsconfig.json`, `convex/_generated/*`.

### 3a. Integration queue — **no pairwise overlap**

`#324`, `#323`, `#322`, `#318`, `#317`, `#304` share **no** meaningful files with each other. They **can** merge in any order without file conflicts with one another.

Cross-links to abandoned PRs (sequence if those were ever revived):

| File | PRs |
|---|---|
| `apps/web/components/tasks/TaskViewsScreen.tsx` | **#318**, #228 |
| `tests/unit/task_filters.test.ts` | **#318**, #228 |
| `apps/web/components/today/TodayScreen.tsx` | **#323**, #228, #217 |
| `.github/workflows/security.yml` | **#317**, #120 |

### 3b. Major overlap groups (2+ PRs, meaningful files)

**Security / auth mega-cluster** (close as duplicates):  
`convex/auth.ts`, `convex/users.ts`, `convex/revenuecat.ts`, `convex/lib/requireUser.ts`, `convex/lib/accountDeletion.ts`, `convex/lib/subscriptionGuards*.ts`, `convex/messages.ts`  
→ PRs **#47, #48, #53, #55, #76, #103, #110, #114, #115, #117, #118, #119, #123, #124, #126, #127, #128, #130, #131** (+ #223 touches requireUser/users).

**Schema-touching feature pile** (all frozen-schema risk + master base):  
`convex/schema.ts` → **#136, #170, #172, #173, #174, #179, #213, #225, #226, #227, #228**  
⚠️ `convex/schema.ts` is **frozen** this month (PR #315). Any revive must avoid schema edits or wait for thaw.

**Calendar cluster:** `calendar/page.tsx`, `date-math.ts`, `calendar_events.ts` → **#225, #226**

**Habits/routines cluster:** habits pages, `convex/habits.ts`, `convex/routines.ts`, streak tests → **#171, #172, #174, #227** (+ mobile #178/#181/#218/#221)

**Mobile navigation / session player cluster:**  
`SessionPlayer`, routines routes, `navigation.spec.ts` → **#178, #180, #181, #208, #214, #215, #218, #220, #221, #222**

**Docs cluster:** README / HARD_RULES / CI docs → **#109, #113, #125, #129** (+ #134 README)

**Workflow cluster:**  
`.github/workflows/ci.yml` → #120, #230  
`.github/workflows/security.yml` → #120, #317

**Connected-component summary (meaningful edges):** one giant 54-PR component of master-era feature+security work; docs component (5); workflow component (3). Integration greens are **outside** that tangle except #318↔#228 and #323↔#217/#228 historical edges.

### 3c. Additional overlaps from #182–#206 batch

| File | PRs |
|---|---|
| `apps/web/app/(tempo)/insights/page.tsx` | **#322**, #185 (scaffold shell mega-PR) |
| `apps/web/app/(tempo)/calendar/page.tsx` | #225, #226, #203, #185 |
| `apps/mobile/app/(tempo)/routines.tsx` | #171, #178, #181, #186, #187, #190, #197, #218, #221 |
| `apps/web/proxy.ts` | many web PRs including #182, #192, #193, #196, #198, #200, #202, #204, #205 |
| `convex/schema.ts` | +#203, #206 (still frozen) |
| `tests/unit/task_filters.test.ts` | **#318**, #228, #206 |

**#322 vs #185:** both touch `insights/page.tsx`. #185 is a huge abandoned Agentwright shell on `master` — **close #185**; do not sequence it with #322.

### 3d. File data coverage

Filename lists collected for **99 / 100** open PRs. Remaining gap: **#122** exact filenames still **UNKNOWN** (title places it in the test-coverage duplicate family — close-recommend stands). Do not block the top-10 merge on it.

---

## 4. agent-writie stack

**Skipped** (per user: “Only do the stuff that is relevant for the tempo/rhythm repo”).

- Merge order: UNKNOWN  
- `ci.yml` job-name strings: UNKNOWN  
- Blockers: UNKNOWN  

---

## 5. Convex `returns:` validator audit

Schema source: `convex/schema.ts` on `integration` (frozen). Relevant stored fields:

**tasks:** `userId, title, description?, status, priority, energy?, timeEstimate?, timeSpentOnDay?, repeatCfgId?, parentTaskId?, projectId?, projectName?, dueAt?, createdAt, updatedAt, deletedAt?`  
**notes:** `userId, title, body, pinned, periodType, aiGenerated?, createdAt, updatedAt, deletedAt?`  
**habits:** `userId, name, cadence, currentStreak, longestStreak, lastCompletedAt?, createdAt, updatedAt, deletedAt?`  
**goals:** `userId, title, description?, targetDate?, progressPercent, status, createdAt, updatedAt, deletedAt?`  
**memories / conversations:** counted only by #324 overview (no row return).

| PR | Function | `returns:` fields | Row shape returned? | Schema fields consumed | Verdict |
|---|---|---|---|---|---|
| **#324** | `analytics.overview` | `tasksTotal, taskTodo, taskDone, tasksDueToday, notesTotal, notesPinned, habitsTotal, goalsActive, goalsTotal, memoriesTotal, coachSessionsTotal` (all `v.number()`) | **No — computed scalars only** | tasks(`status,dueAt`); notes(`pinned`); goals(`status`); counts of habits/memories/conversations | **OK** — cannot omit stored row fields by construction. Note: still counts soft-deleted rows (pre-existing; not changed). |
| **#322** | `insights.summary` | `tasksOpen, tasksDueToday, tasksOverdue, tasksCompletedThisWeek, openByEnergy{low,medium,high}, openByPriority{low,medium,high}, habitsTotal, habitsWithActiveStreak, bestStreak, goalsActive, goalsAverageProgressPercent` | **No — computed scalars only** | tasks(`status,priority,energy,dueAt,updatedAt,deletedAt`); habits(`currentStreak,longestStreak,deletedAt`); goals(`status,progressPercent,deletedAt`) | **OK** — filters `deletedAt === undefined`; scalars only. |
| **#323** | *(none new)* | n/a — uses existing `tasks.list` + `tasks.update` | n/a | n/a | **N/A** — no validator authored. |
| **#318** | *(none new)* | n/a | n/a | n/a | **N/A** |
| **#317** | *(none)* | n/a | n/a | n/a | **N/A** |
| **#321** (merged) | `coach.sendMessage` | `success: v.literal(true)` per PR body | no table rows | n/a | Landed; not re-audited beyond PR description. |

No top-10 PR returns full table documents without listing schema fields. The recent bug class (omitting `userId`/`deletedAt` or `timeEstimate`/`timeSpentOnDay`/`repeatCfgId`/`parentTaskId` on row returns) does **not** appear in #322/#324.

---

## 6. Check failures (actual log lines)

### Integration greens (#324, #323, #322, #318, #317)

No failed required checks on latest head SHAs. All of: `Lint`, `Test`, `Typecheck`, `Scans (forbidden-tech, ram-only-audit, design-tokens)`, `Third-party notices (TF-OSS-00)`, secret-scan jobs → **success**.

### #304

Only `Vercel Preview Comments` present. **CI suite absent on recorded checks** — not a failure line; coverage gap.

### #116 Lint — **CODE**

```
app/(tempo)/error.tsx:15:5 lint/suspicious/noConsole
  > 15 │     console.error("Tempo boundary caught error:", error);

app/(tempo)/error.tsx:36:50 lint/suspicious/noAssignInExpressions
  > 36 │           <Button variant="ghost" onClick={() => window.location.href = "/"}>
error: script "lint" exited with code 1
Failed:    tempo-rhythm-web#lint
```

### #116 Typecheck — **CODE**

```
../../convex/auth.ts(108,69): error TS2367: This comparison appears to be unintentional because the types '"user" | undefined' and '"founder"' have no overlap.
Failed:    tempo-rhythm-mobile#typecheck
```

(Job used `bun install v1.3.14` — pin drift vs current `1.3.9`, but the failure itself is a TS error = CODE.)

### #118

Lint + Typecheck **failure** (same era as #116). Exact lines: same class as #116 family on old master — treat as **CODE**. Full twin log not re-pulled; do not merge.

### #302

Lint, Typecheck, Test, Scans **failure** (dependabot mass bump). Exact failing log line: **UNKNOWN** (workflow run lookup by guessed branch name returned 0 runs; head branch name truncated in inventory). Label: **CODE** (dependency bump breakage), not the bun 1.3.14 503 signature.

### Infrastructure flake signature (bun 1.3.14 503)

**Not observed** on the integration PR check runs sampled this session (those jobs succeeded; TF-OSS-00 and secret-scan setup on current `integration` are pinned via #320).  
**Occurrence count this session:** **0** with the exact line:

```
##[error]Error: Unexpected HTTP response: 503 from oven-sh/setup-bun@v2
```

Historical note: old runs (e.g. #116 typecheck) still show `bun install v1.3.14` from `bun-version: latest` on that commit’s workflows.

---

## 7. Un-stale attempt (top candidates)

Policy: merge `origin/integration` **into** each head (no rebase, no force-push).

| PR | Local merge of `integration` → head | Push to head branch |
|---|---|---|
| #324 | **CLEAN** | **BLOCKED** HTTP 403 `send-pack` |
| #318 | **CLEAN** | not pushed (same permission) |
| #317 | **CLEAN** | not pushed |
| #323 | **CLEAN** | not pushed |
| #322 | **CLEAN** | not pushed |
| #304 | **CLEAN** | not pushed |

Conflict count during ranked merges: **0** (stop rule of >3 conflicts not triggered).

**Remote heads were not updated.** Verified for #324: local merge commit `adcf4e4` / `efa564e` ≠ remote `50c92388`.

### What stopped the un-stale

- **Branch push permission / ruleset:** `git push origin HEAD:refs/heads/<pr-head>` → `error: RPC failed; HTTP 403`.  
  This is the rule working: the triage agent may only push its own agent branch (`copilot/triage-open-prs`) via the progress tool, not foreign PR heads.
- **Did not** force-push, rebase, or route around the 403.
- **Did not** edit `.github/workflows/**`, rulesets, or `convex/schema.ts`.
- **Did not** merge or approve any PR.

**Human un-stale one-liner per PR (local):**

```bash
git fetch origin integration cursor/<head>
git checkout cursor/<head>
git merge origin/integration   # no rebase
git push origin HEAD
```

---

## 8. Anything not done / UNKNOWN

| Item | Status |
|---|---|
| Push un-stale commits to PR heads | **UNKNOWN / blocked** — HTTP 403 on foreign branches |
| agent-writie Task B entirely | **UNKNOWN** — out of scope by instruction |
| Exact filenames for #122 | **UNKNOWN** (non-blocking; duplicate test-coverage family) |
| #302 exact failing log line | **UNKNOWN** |
| Whether #223 brain-dump diff is fully subset of trunk | **UNKNOWN** — close only after human skim |
| Re-run of infrastructure-flaky jobs | N/A — 0 flakes seen on sampled integration PRs |
| Mass-close execution | Not done (recommend only) |
| Retarget of 94 `master` PRs to `integration` | Not done (would be human/product call; most should close) |

---

## 9. Inventory (compact)

Format: `#num | base | draft | age_d | stale_d | author | head | title`

```
#324 | integration | draft | 0.5 | 0.5 | Division6066 | cursor/analytics-returns-3a0b | fix(analytics): returns validator
#323 | integration | draft | 0.5 | 0.5 | Division6066 | cursor/energy-recommendations-3a0b | feat(today): energy recommendations
#322 | integration | draft | 0.5 | 0.5 | Division6066 | cursor/insights-summary-3a0b | feat(insights): summary query + screen
#318 | integration | draft | 0.7 | 0.7 | Division6066 | cursor/fix-project-slug-filter-2ad0 | fix(tasks): projectSlug normalize
#317 | integration | ready | 1.1 | 0.6 | Copilot | copilot/fix-cursor-bugbot-issues | fix: secret-scan fixture exit codes
#304 | integration | draft | 11.0 | 11.0 | Division6066 | cursor/vercel-preview-pipe-3d62 | chore: Vercel preview pipe
#302 | master | ready | 17.2 | 3.2 | dependabot[bot] | dependabot/npm_and_yarn/tempo-workspace-* | chore(deps): 35 workspace updates
#230 | master | ready | 24.2 | 24.2 | dependabot[bot] | dependabot/github_actions/actions/setup-* | chore(deps): setup-node 4→7
#229–#170 | master | draft | ~29.7 | ~29.7 | Division6066 | cursor/* | feature drafts (mobile/web)
#137–#134 | master | draft | ~30–31 | ~30–31 | Division6066 | cursor/* | e2e/consent/docs
#133 | master | ready | 31.7 | 31.6 | Division6066 | cursor/bc-0dbf1b1a-* | feat(backend): @tempo/backend package
#132 | master | draft | 48.8 | 48.8 | nebula-ai-agent[bot] | TEMPO-126-qwen-trial-1 | disposable shakeout
#131–#103 | master | draft | ~50–56 | ~50–56 | cursor[bot] | cursor/critical-*|missing-test-*|docs-* | duplicate security/tests/docs
#120 | master | ready | 52.2 | 52.2 | dependabot[bot] | dependabot/github_actions/actions/checko* | chore(deps): checkout 4→7
#119–#109 | master | mix | ~52–54 | ~52–54 | cursor/nebula | various | auth/security/docs/tests
#103–#47 | master | draft | ~56–69 | ~57.5 | cursor[bot] | cursor/* | older security/test duplicates
#79 | master | draft | 61.5 | 57.5 | Division6066 | cursor/design-orchestrator-phase0-* | docs(design) inventory
```

Full machine-readable inventory: generated during the run as `/tmp/pr_inventory.json` (not committed).

---

## 10. Rules that stopped work (working as designed)

1. **Cannot push to branches this agent did not own** → un-stale merges stayed local (403).  
2. **Do not edit `.github/workflows/**`** → did not “fix” #317’s workflow hunk or dependabot workflow PRs.  
3. **Do not touch `convex/schema.ts`** → frozen; noted schema-touching abandoned PRs as close/rewrite.  
4. **Do not merge / approve / auto-merge / close** → report-only actions on the queue.  
5. **Do not change #322–#324** (and #321 already merged) → ranked only.  
6. **`master` is not the trunk** → 94 PRs flagged wrong-base rather than merged.  
7. **PR opener defaulted triage doc PR #326 base to `master`.** Retarget to `integration` was attempted via GitHub API but **no write-capable API token** is available to this agent (`git credential` returns username only; `GH_TOKEN` unset; Copilot tokens 401 on REST). **Human must run:** `gh pr edit 326 --base integration`. Until then the GitHub UI diff vs `master` is inflated (includes all of `integration` not in `master`); the only intentional file in this triage PR is `docs/PR_TRIAGE_2026-08-13.md`.

---

## 11. Graph / docs contradictions

- Rules point at `docs/brain/TASKS.md` (private submodule) — **empty for cloud agents**. Used Linear / in-repo signals instead.  
- Problem text treated #321–#324 as open queue; **#321 is already merged** on `integration`.  
- README package-manager drift (pnpm vs Bun) unchanged; not in scope.

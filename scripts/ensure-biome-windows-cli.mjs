#!/usr/bin/env node
/**
 * Postinstall hook: reserved for Windows-specific Biome CLI setup if needed.
 * Keeps `bun install` working when this file must exist (see root package.json).
 */
process.exit(0);
